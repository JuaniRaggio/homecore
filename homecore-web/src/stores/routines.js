import { defineStore } from 'pinia'
import { ref } from 'vue'

// Store de rutinas

export const useRoutinesStore = defineStore('routines', () => {
  // const routines = ref([])
  // const loading = ref(false)

  // const favoriteRoutines = computed(() => routines.value.filter(r => r.isFavorite))

  // async function fetchRoutines() { /* api.getRoutines() */ }
  // async function execute(routineId) { /* api.executeRoutine() */ }
  // async function create(data) { /* api.createRoutine() */ }
  // async function remove(routineId) { /* api.deleteRoutine() */ }

  return {}
})
